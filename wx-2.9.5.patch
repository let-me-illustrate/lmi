diff --recursive '--unified=3' original/wxWidgets/include/wx/cpp.h modified/wxWidgets/include/wx/cpp.h
--- original/wxWidgets/include/wx/cpp.h	2012-12-03 01:30:15.000000000 +0000
+++ modified/wxWidgets/include/wx/cpp.h	2013-07-11 23:24:28.000000000 +0000
@@ -134,6 +134,13 @@
 #endif
 
 #ifdef wxHAS_VARIADIC_MACROS
+/* There is no other way to suppress the
+ *  "anonymous variadic macros were introduced in C99"
+ * diagnostic with gcc-3.x .
+ */
+#  if defined(__GNUC__) && __GNUC__ == 3
+#    pragma GCC system_header
+#  endif /* gcc-3.x */
 /*
    wxCALL_FOR_EACH(what, ...) calls the macro from its first argument, what(pos, x),
    for every remaining argument 'x', with 'pos' being its 1-based index in
